spring:
  data:
    mongodb:
      database: testdb

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2

mongock:
  change-logs-scan-package: ru.otus.hw.changelog
  mongo-db:
    write-concern:
      journal: false
    read-concern: local

